spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/gca-core-test
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
  activemq:
    broker-url: tcp://activemq:61616
    user: test
    password: test

jwt:
  secret: ${JWT_SECRET:test-key}

logging:
  level:
    root: INFO
    org.springframework: DEBUG
    com.gcs.app: DEBUG
