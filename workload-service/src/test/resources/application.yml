spring:
  data:
    mongodb:
      database: testdb
  activemq:
    broker-url: vm://embedded?broker.persistent=false

eureka:
  client:
    enabled: false

jwt:
  secret: test-key

jms:
  queue:
    trainer-workload: trainer.workload.queue
  listener:
    concurrency: "3-10"
