spring:
  data:
    mongodb:
      database: testdb
  activemq:
    broker-url: vm://embedded?broker.persistent=false
    user: ""
    password: ""

eureka:
  client:
    enabled: false

jwt:
  secret: gym-crm-application-SECRETKEY123!@#

jms:
  queue:
    trainer-workload: trainer.workload.queue
  listener:
    concurrency: "3-10"
