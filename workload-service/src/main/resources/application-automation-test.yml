spring:
  data:
    mongodb:
      uri: mongodb://mongodb:27017/testdb?replicaSet=docker-rs
  activemq:
    broker-url: tcp://activemq:61616
    user: admin
    password: admin

eureka:
  client:
    enabled: false

jwt:
  secret: ${JWT_SECRET:test-key}